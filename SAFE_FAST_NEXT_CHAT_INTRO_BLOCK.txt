START HERE - SAFE-FAST NEXT CHAT OPERATING RULES

Read SAFE_FAST_BUILD_STATE.md first.

Then verify local branch, commit, and status:

Set-Location "C:\Users\nickm\Desktop\New folder\safe-fast-backendnew"
git --no-pager branch --show-current
git --no-pager log -1 --oneline
git --no-pager status --short

CURRENT DAY 50 ENTRY POINT:

- Current canonical registry: `SAFE_FAST_DATA_SOURCE_REGISTRY.md`
- Machine registry: `historical_signal_replay/config/safe_fast_data_source_registry.json`
- Resolver: `watcher_foundation/safe_fast_data_source_resolver.py`
- Current result: `SAFE_FAST_DAY50_DATA_SOURCE_REGISTRY_AND_SCHWAB_QUEUE_RESULT.md`
- Exact active task: `SAFE_FAST_DAY50_SCHWAB_READ_ONLY_AUTH_AND_CAPABILITY_AUDIT_CODEX_TASK.md`

Do not report vague MISSING_DATA. Every blocker must state exact field, exact source, exact dataset/schema/API/calculator, exact timestamp window, exact reason unavailable, whether it blocks setup/trade/execution/exit or only optional context, and exact next action.

Charles Schwab is the live broker/account/order/fill authority. Databento is the primary historical options source. SAFE-FAST setup labels stay inside the SAFE-FAST frozen local rule engine.

Required source rules:
- Historical underlying: Databento `DBEQ.BASIC / ohlcv-1h / raw_symbol`.
- Historical options: Databento `OPRA.PILLAR`.
- Option quote freshness: `cmbp-1`; `cbbo-1s` only after explicit validation.
- `tcbbo` is supplemental and must not be the sole quote-freshness source.
- Live fills: Schwab controls. Databento, tastytrade, and TradingView cannot override Schwab fills.
- Volatility: SAFE-FAST calculated realized volatility and option IV/Greeks when inputs exist; Cboe VIX/VIX9D, VXN, RVX, and GVZ for official index context.
- News/events: SEC, Federal Reserve, BLS, BEA, Treasury, issuer IR; Benzinga only when API entitlement and credentials exist.
- Macro: ALFRED for historical vintages. Do not silently use revised current macro values.

Current blocker status:
- The eight Day 49 candidates are not no-trades.
- They are mapped as `4` exact external setup-data required, `3` source conflicts, and `1` source-unavailable candidate exclusion.
- Do not run another incomplete candidate batch.
- Optional context cannot silently block setup qualification unless a frozen rule explicitly makes it mandatory.

Grouped work must preserve a funnel scorecard and keep these categories separate: VALID_TRADE_CAPTURED, TRUE_NO_TRADE, MISSING_DATA, MISSED_VALID_TRADE, INVALID_TRADE_ALLOWED, and UNRESOLVED.

Do not download paid data without an exact cost check and user approval.

Correct Codex launch command for the next task:

codex.cmd -c 'windows.sandbox="unelevated"' --sandbox workspace-write --ask-for-approval never "Read .\SAFE_FAST_BUILD_STATE.md first, then read and execute .\SAFE_FAST_DAY50_SCHWAB_READ_ONLY_AUTH_AND_CAPABILITY_AUDIT_CODEX_TASK.md exactly."

Do not authenticate to Schwab until the task reaches the explicit OAuth browser authorization step. Do not submit, replace, cancel, preview, or call any order endpoint.

Use this format for build work:
Baseline:
Fixed:
Blocked:
Next:
Tests:

END START BLOCK
